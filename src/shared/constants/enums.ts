export enum ENotificationStatus {
  Pending = 'pending',
  Sent = 'sent',
  Error = 'error',
  Clicked = 'clicked',
  Read = 'read',
}

export enum ESegmentStatus {
  Active = 'active',
  Inactive = 'inactive',
}
